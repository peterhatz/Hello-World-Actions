on: [push]
jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: Get the output time
        - run:  bash ${GITHUB_WORKSPACE}/path/script.sh
        - shell: bash
